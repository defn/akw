#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  runmnay 'reality $1' recycle 'swarm primary'
  runmany '$1 recycle; $1 swarm' van hotel fortress
}

source sub "$BASH_SOURCE" "$@"
