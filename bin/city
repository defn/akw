#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"
  source "$shome/exec/inception"

  export BASEBOX_INSTANCE='city'

  exec limbo aws "$@"
}

source sub "$BASH_SOURCE" "$@"
