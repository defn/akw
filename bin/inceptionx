#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  eval $(planex docker machine env --swarm --no-proxy planex)
  exec docker "$@"
}

source sub "$BASH_SOURCE" "$@"
