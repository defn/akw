#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"
  source "$shome/exec/akw_common"

  export BASEBOX_INSTANCE="$(basename "$BASH_SOURCE")"
  export BASEBOX_IP='172.28.128.6'
  exec akw "$@"
}

source sub "$BASH_SOURCE" "$@"
