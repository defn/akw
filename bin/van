#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  export BASE_INSTANCE="${0##*/}"
  exec "$shome/script/limboX" virtualbox "$@"
}

source sub "$BASH_SOURCE" "$@"
