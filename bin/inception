#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  eval $(plane docker machine env --swarm --no-proxy plane)
  exec docker "$@"
}

source sub "$BASH_SOURCE" "$@"
